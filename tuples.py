#tuples are data structures which is similar to list .
# the major b/w list are mutable that could be change the conntent of a list
#however can't change the content of a tuple once the tuple declared
fruits =('apple', 'mango','banana','grapse','watermelon','corn')

print(f"tuple {fruits}")
 # fruits[1]="haro"   # not working tuple' object does not support item assignment

 #tuple slicing

print(f"tuple slicing {fruits[1:5]}")